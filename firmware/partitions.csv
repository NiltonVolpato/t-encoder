# Custom partition table for enc-app. Adds a dedicated `settings` data
# partition (located at runtime by label) so persisted app settings never touch
# the standard `nvs` partition. Offsets/sizes are hex.
# Name,     Type, SubType, Offset,   Size
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0xFEC000,
settings,   data, nvs,     0xFFC000, 0x4000,
