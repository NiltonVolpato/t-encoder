// Copyright © 2026 Nilton Volpato
// SPDX-License-Identifier: MIT

import { AppWindow, Theme, CircularScreen, AppInfo, TextSm, TextXlBold } from "@theme";

export global SonosInfo {
    out property <AppInfo> info: {
        name: "Sonos",
        accent: #f59e0b,
        icon: @image-url("../assets/sonos.svg"),
    };
}

export component SonosApp inherits AppWindow {
    title: "Sonos";
    background: #f59e0b;

    callback exit();

    forward-focus: dial-focus;
    dial-focus := FocusScope {
        key-pressed(event) => {
            if event.text == Key.Escape {
                root.exit();
                return accept;
            }
            reject
        }
    }

    CircularScreen {
        background: #f59e0b;

        TouchArea {
            clicked => { root.exit(); }
        }

        VerticalLayout {
            alignment: center;
            spacing: 12px;

            TextXlBold {
                text: "Sonos";
                color: white;
                horizontal-alignment: center;
            }

            TextSm {
                text: "TAP OR ESC TO EXIT";
                color: white.darker(0.2);
                horizontal-alignment: center;
                letter-spacing: 1.5px;
            }
        }
    }
}

