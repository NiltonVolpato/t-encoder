// Copyright © 2026 Nilton Volpato
// SPDX-License-Identifier: MIT

import { AppWindow, Theme, CircularScreen, AppInfo, TextSm, TextXlBold } from "@theme";

export global SimonInfo {
    out property <AppInfo> info: {
        name: "Simon",
        accent: #10b981,
        icon: @image-url("../assets/simon.svg"),
    };
}

export component SimonApp inherits AppWindow {
    title: "Simon";
    background: #10b981;

    callback exit();

    forward-focus: dial-focus;
    dial-focus := FocusScope {
        key-pressed(event) => {
            if event.text == Key.Escape {
                root.exit();
                return accept;
            }
            reject
        }
    }

    CircularScreen {
        background: #10b981;

        TouchArea {
            clicked => { root.exit(); }
        }

        VerticalLayout {
            alignment: center;
            spacing: 12px;

            TextXlBold {
                text: "Simon";
                color: white;
                horizontal-alignment: center;
            }

            TextSm {
                text: "TAP OR ESC TO EXIT";
                color: white.darker(0.2);
                horizontal-alignment: center;
                letter-spacing: 1.5px;
            }
        }
    }
}

