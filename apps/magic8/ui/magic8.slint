// Copyright © 2026 Nilton Volpato
// SPDX-License-Identifier: MIT

import { AppWindow, Theme, CircularScreen, AppInfo, TextSm, TextXlBold } from "@theme";

export global Magic8Info {
    out property <AppInfo> info: {
        name: "Magic 8",
        accent: #8b5cf6,
        icon: @image-url("../assets/magic8.svg"),
    };
}

export component Magic8App inherits AppWindow {
    title: "Magic 8";
    background: #8b5cf6;

    callback exit();

    forward-focus: dial-focus;
    dial-focus := FocusScope {
        key-pressed(event) => {
            if event.text == Key.Escape {
                root.exit();
                return accept;
            }
            reject
        }
    }

    CircularScreen {
        background: #8b5cf6;

        TouchArea {
            clicked => { root.exit(); }
        }

        VerticalLayout {
            alignment: center;
            spacing: 12px;

            TextXlBold {
                text: "Magic 8";
                color: white;
                horizontal-alignment: center;
            }

            TextSm {
                text: "TAP OR ESC TO EXIT";
                color: white.darker(0.2);
                horizontal-alignment: center;
                letter-spacing: 1.5px;
            }
        }
    }
}

