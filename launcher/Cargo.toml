[package]
name = "launcher"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Pure app-launcher core: App trait, router, layout. No hardware."

[dependencies]
embedded-graphics = { workspace = true }
# Pure, host-testable upstream crates: the Dirty/InputEvent vocabulary and the
# shared atomic state.
enc-ui = { workspace = true }
enc-state = { workspace = true }

[lints]
workspace = true
